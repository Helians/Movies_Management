1.Run the database script using ssms(SQL server management studio),

2.Open the solution.

3.In the connection string of DeltaXDataAccessLayer which is present in App.config,change your data sourxe and initial catalog.

4.Repeat the step 3 for web.config in DeltaXWebApplication.

5.Add all the references needed.

6.Set the Web application as start project.

7.Run in chrome(preffered).